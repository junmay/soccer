<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="soccer.hello.login.Repository.mybatis.LeagueMapper">
    <select id="getAllLeagues" resultType="League">
        SELECT league_id, league_name, league_location FROM league
    </select>
</mapper>
